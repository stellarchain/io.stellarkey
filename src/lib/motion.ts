/** Must match `--motion-duration-standard` in `globals.css`. */
export const MODAL_EXIT_DURATION_MS = 180;
/** Must match `--motion-duration-sheet-out` in `globals.css`. */
export const MODAL_SHEET_EXIT_DURATION_MS = 260;
/** Must match `--motion-duration-fast` in `globals.css`; the crossfade used under Reduce Motion. */
export const REDUCED_MOTION_EXIT_DURATION_MS = 120;
/** Must match `--motion-duration-fast` in `globals.css`. */
export const POPOVER_EXIT_DURATION_MS = 120;
/** Must match `--motion-duration-standard` in `globals.css`. */
export const TOAST_EXIT_DURATION_MS = 180;
/** Must match `--motion-duration-fast` in `globals.css`; brief fades such as the privacy shield leaving. */
export const MOTION_FAST_DURATION_MS = 120;
