[package]
name = "private-balance-archive-gate"
version = "0.1.0"
edition = "2024"
rust-version = "1.97.1"
publish = false

[lib]
crate-type = ["cdylib", "rlib"]

[dependencies]
soroban-sdk = { version = "=27.0.6", default-features = false }

[dev-dependencies]
soroban-sdk = { version = "=27.0.6", features = ["testutils"] }
